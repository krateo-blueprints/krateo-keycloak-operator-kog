{{- if .Values.auth.externalSecret.enabled }}
{{- /*
External Secrets Operator wiring for the short-lived Keycloak admin bearer token.

Flow:
  ESO Webhook generator --(POST client_credentials)--> Keycloak token endpoint
       |  extracts $.access_token
       v
  ExternalSecret --(every refreshInterval)--> Secret/<auth.secretName> (key: <auth.secretKey>)
       ^
       |  referenced by each <Kind>Configuration.spec.authentication.bearer.tokenRef
  rest-dynamic-controller

NOTE: ESO generator field names vary across ESO versions. Validated shape below
targets external-secrets >= 0.10. Adjust apiVersions if your cluster differs.
The service-account client (auth.clientId) must hold the `realm-management` roles
needed for the resources you manage (e.g. manage-clients, manage-realm, manage-users).
*/ -}}
apiVersion: generators.external-secrets.io/v1alpha1
kind: Webhook
metadata:
  name: {{ .Values.auth.externalSecret.generatorRef.name }}
  labels:
    {{- include "keycloak-config-kog.labels" . | nindent 4 }}
spec:
  url: {{ .Values.auth.externalSecret.tokenEndpoint | quote }}
  method: POST
  headers:
    Content-Type: application/x-www-form-urlencoded
  # ESO exposes each `secrets` entry to the body template by NAME then KEY
  # (dot-name-dot-key). The entry below is named `kog` with key `clientSecret`,
  # so the body interpolates it as .kog.clientSecret.
  # (clientSecretRef.key must be `clientSecret` for this reference to resolve.)
  body: "grant_type=client_credentials&client_id={{ .Values.auth.externalSecret.clientId }}&client_secret={{ `{{ .kog.clientSecret }}` }}"
  secrets:
    - name: kog
      secretRef:
        name: {{ .Values.auth.externalSecret.clientSecretRef.name }}
        key: {{ .Values.auth.externalSecret.clientSecretRef.key }}
  result:
    # Return the WHOLE token response object ("$") so the ExternalSecret template
    # can pick `.access_token`. A jsonPath to the scalar token would make ESO try
    # to parse the JWT string as JSON and fail.
    jsonPath: "$"
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ .Values.auth.secretName }}
  labels:
    {{- include "keycloak-config-kog.labels" . | nindent 4 }}
spec:
  # Keep this BELOW the Keycloak access-token lifespan so the bearer never goes stale.
  refreshInterval: {{ .Values.auth.externalSecret.refreshInterval | quote }}
  target:
    name: {{ .Values.auth.secretName }}
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        {{ .Values.auth.secretKey }}: "{{ `{{ .access_token }}` }}"
  dataFrom:
    - sourceRef:
        generatorRef:
          apiVersion: {{ .Values.auth.externalSecret.generatorRef.apiVersion }}
          kind: {{ .Values.auth.externalSecret.generatorRef.kind }}
          name: {{ .Values.auth.externalSecret.generatorRef.name }}
{{- if .Values.restDefinitions.authenticationexecution.enabled }}
---
{{- /*
Second projection of the SAME rotated admin token, in the snowplow `Endpoint`
Secret shape (server-url + token), consumed via `endpointRef` by the
KeycloakAuthenticationExecution RESTActions
(templates/restactions-authenticationexecution.yaml).
*/}}
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ .Values.snowplow.endpointSecretName }}
  labels:
    {{- include "keycloak-config-kog.labels" . | nindent 4 }}
spec:
  refreshInterval: {{ .Values.auth.externalSecret.refreshInterval | quote }}
  target:
    name: {{ .Values.snowplow.endpointSecretName }}
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        server-url: {{ .Values.keycloak.baseUrl | quote }}
        token: "{{ `{{ .access_token }}` }}"
  dataFrom:
    - sourceRef:
        generatorRef:
          apiVersion: {{ .Values.auth.externalSecret.generatorRef.apiVersion }}
          kind: {{ .Values.auth.externalSecret.generatorRef.kind }}
          name: {{ .Values.auth.externalSecret.generatorRef.name }}
{{- end }}
{{- end }}
